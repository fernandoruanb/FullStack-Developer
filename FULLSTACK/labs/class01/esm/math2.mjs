export function div(a, b) { return a / b };
export default function mul(a, b) { return a * b };
export const sub = (a, b) => a - b;
export const add = (a, b) => a + b;

// It is only possible to have just one default per module
